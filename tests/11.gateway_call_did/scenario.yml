---
timeout: 30

tasks:
  - name: Register callee
    use: register_callee

  - name: Callee UAS
    use: callee_uas

  - name: Gateway UAC
    use: gateway_uac
    keys:
      callee_did: {{ callee_did }}
    require:
      started: Callee UAS

cleanup_tasks:
  - use: delete_callee_aor
