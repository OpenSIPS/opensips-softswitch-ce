---
timeout: 40

tasks:
  - name: Register caller
    use: register_caller

  - name: Caller UAC
    use: caller_uac
    require:
      - after:
          task: Register caller

cleanup_tasks:
  - use: delete_caller_aor
